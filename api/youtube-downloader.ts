// This file is no longer needed as the YouTube downloader functionality 
// has been moved to the main server.ts file to avoid conflicts and 
// ensure proper server-side execution of yt-dlp commands.

export default function handler() {
  // Functionality moved to server.ts
}